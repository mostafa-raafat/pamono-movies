import { createMuiTheme } from '@material-ui/core/styles';

// A custom theme for Pamono Movie App.
const theme = createMuiTheme({
  palette: {
    primary: {
        light: '',
        main: '#f0f',
        dark: '',
        contrastText: "#FFFFFF"
    }
  }
});

export default theme;